# RECTIFY v1.0.0 — Master Plan & Dispatch Map

**Date:** 2026-05-23
**Branch:** `drs-validation-rebuild` (M1 HEAD `56bcee6`; origin `5b348ec` → only E.3 unpushed)
**Goal (per Kevin):** finish remaining improvements on M1 → validate at production scale on Sherlock + H2 → push/commit to GitHub as **RECTIFY v1.0.0**.

This is the coordinating plan for the v1.0.0 push. It reconciles the (stale) `parallel_refactor_plan.md` checkboxes and `BUGS_TO_FIX.md` "Open" section against **verified** git/code/cluster state, then buckets remaining work into parallel (Sonnet dispatch) vs sequential (Opus master).

---

## 0. Cluster authority finding (answers "is M1 authoritative?")

**Yes — M1 is authoritative by construction. No pull-back needed.** Verified 2026-05-23:

- H2 `/u/home/k/kevinroy/software/rectify` and Sherlock `/oak/.../software/rectify` are both **non-git rsync deployments** (no `.git`; only sibling projects under `software/` have `.git`).
- Same 166 `rectify/**/*.py` files on both M1 and Sherlock; **no file exists only on a cluster.**
- 10 files differ M1↔Sherlock; all are **M1-ahead** (Sherlock lacks `reuse_pool_container`/C-II.1, `no-parallel-aligners`/E.3, `_stage_raw_bams`/Lustre f7e6463; shares older lazy-consensus + `_prescan_bam_once` + Module-2H code).
- `junction_scoring.py`, `junction_refiner.py`, `prescan_command.py` are **byte-identical** M1↔Sherlock → the 2026-05-21 Module-2H work (candidate cap, bisect lookup, `--junction-max-size`) is consistent.

**Implication:** clusters are deployment targets. Before any production-scale test, rsync M1→cluster (`.codex_backups` taken first per established pattern). Never commit on a cluster.

---

## 1. Verified status (DONE — code-confirmed, not just inferred)

| Area | Commit(s) | Verified by |
|---|---|---|
| Lazy corrected consensus + HP realign | `1ab71f0` | feature present; all 9 audit findings in `lazy_corrected_consensus_audit_20260521.md` fixed |
| Lustre cold-read staging (`_stage_raw_bams`) | `f7e6463` | grep present |
| C-II.0 worker-state split | `5db2112` | byte-equiv golden test |
| C-II.1 shared Pool (`reuse_pool_container`) | `55dd212` | grep present in correct_command/stages/parallel |
| Commit D reanchor O(CIGAR) | `55089f7` | committed |
| analyze + restore_polya sidecar wiring | `8c9f80c` | committed |
| E.1 drs_trim parallel + sidecar, E.2 cdna_stage1 | `9b4bb56` | `test_drs_trim_sidecar.py`, `test_cdna_stage1_sidecar.py` |
| E.3 parallel-aligners default + align sidecar | `56bcee6` | grep `no-parallel-aligners` |
| Provenance/sidecar/resume infra | (multiple) | `rectify/core/provenance/{sidecar,skip_check,path_resolver,hashing,cluster,cli}.py` + `bam/{regions,bam_writer_parallel,tsv_partition}.py` + `utils/resources.py` all exist on disk |
| Bedgraph 1-bp shift **NEW-076** (3 instances) | committed | analyze/bedgraph.py, analyze/manifest.py, scripts/ all fixed |
| **NEW-067** index-before-replace | committed | align_command.py:34-36 |
| **NEW-068** minimap2-required raise | committed | multi_aligner.py:2103 |
| **NEW-069** `.done` sentinel validates BAM | committed | consensus.py:137 `_validate_bam_sample`, called :863 |
| **NEW-070** atomic `Path.replace()` | committed | single_sample.py:343,790 |
| **NEW-071** samtools fastq stderr preserved | committed | single_sample.py:36-55 |
| **NEW-073** single prescan pass | committed | bam/parallel.py:262 `_prescan_bam_once` |
| **NEW-074** fsync after copy2 | committed | consensus.py:131 `_copy2_and_fsync` |
| **NEW-066** uLTRA stale-cache guard | committed | multi_aligner run_ultra |
| QNAME sanitizer/validator + RN sidecar | committed | per AGENT_FIXES 2026-05-20 |
| Module 2H candidate-cap / bisect / max-size | committed | junction_scoring/refiner; identical on Sherlock |

**Doc-reconciliation note:** `BUGS_TO_FIX.md` still lists NEW-066–074 + NEW-076 under "Open" — that section is **stale**; they are landed. See P1.

---

## 2. Remaining work — PARALLEL (Sonnet dispatch agents)

**Concurrency rule (mandatory):** multiple agents must NOT `git commit` into the single M1 repo concurrently (see `feedback_concurrent_sessions_git_race`). Therefore Sonnet agents **edit disjoint files + run their focused test slice, then report the diff — they do NOT commit.** The Opus master stages + commits each atomically in sequence (explicit paths, never `git add -A`). Larger items may instead use `isolation: worktree`. **No agent pushes.**

| ID | Task | Files (verify line #s) | Why parallel-safe |
|---|---|---|---|
| **P1** | Doc reconciliation: move NEW-066–074 + NEW-076 to "resolved" in `dev/BUGS_TO_FIX.md`; update `HANDOFF.md` (only E.3 unpushed; C-II.1 pushed); retire `parallel_refactor_plan.md` A/B/C/D/E/F checkbox taxonomy → actual commit taxonomy | `dev/BUGS_TO_FIX.md`, `HANDOFF.md`, `dev/specs/parallel_refactor_plan.md` | read-only audit + one write-back per file |
| **P2** | **NEW-079** plus-strand `distance_to_gene_3prime` off-by-1 (`gene['end']` → `gene['end']-1`) | `clustering.py:532` | 1-line, isolated |
| **P3** | **NEW-081** rDNA exclusion `<= end` → `< end` | `analyze_command.py:130`, `analyze/manifest.py:217` | isolated |
| **P4** | **NEW-072** verify-then-fix deSALT/uLTRA stderr drain. **Likely a NO-OP**: filtered grep shows the run_ultra (~1752) and run_desalt (~1962) sort_proc Popens use `stdout=PIPE` but **no `stderr=PIPE`** → stderr inherited → no deadlock. Agent must confirm by reading those exact lines; if stderr is inherited, close NEW-072 as not-a-bug — **do not invent a drain thread.** Fix only if `stderr=PIPE` without a reader is actually present. | `multi_aligner.py` run_ultra (~1566), run_desalt (~1818) | isolated to one module |
| **P5** | TODO: surface `stats['tied_score']` + `stats['by_aligner_combo']` into HTML report + `corrected_3ends_stats.tsv` | `consensus/consensus.py` + report template | additive, isolated |
| **P6** | TODO: emit `cluster_com` (read-weighted center-of-mass) column in analyze cluster table; retire `62_compute_cluster_com.py` | `analyze_command.py` | additive column |
| **P7** | Doc consolidation Buckets 2,3,5,6,7 (git mv only) — **defer Bucket 4** (AGENT_FIXES/HANDOFF moves) until WIP committed; run LAST to avoid path-thrash with P1 | per `dev/reviews/doc_consolidation_proposal_20260522.md` | pure `git mv`; sequence after P1 |

---

## 3. Remaining work — SEQUENTIAL (Opus master agent)

Algorithmically coupled, concurrency-sensitive, or output-changing — must be reasoned about as a unit.

| ID | Task | Files / spec | Priority |
|---|---|---|---|
| **S0** | **Clean the working tree FIRST** (before any dispatch). Commit current mods `AGENT_FIXES.md` + `HANDOFF.md` (atomic, explicit paths). Triage the 3 untracked items individually — do NOT blanket-add: `dev/branding/`, `handoffs/` (root dir slated for Bucket-6 consolidation), `scripts/timing/phase_c_lazy_timing_20260521.sh`. P1 and P7/Bucket-4 depend on this. | working tree | FIRST |
| **S1** | **Minus-strand artifact-N coordinate cluster** — NEW-077 (`bam_processor.py:826-836` N-snap lands on skipped base), NEW-078 (`:847` ambiguity clip leaves NET-seq window in N span), NEW-080 (`false_junction_filter.py:282 vs 300` wrong-flank). **Same root half-open-vs-aligned-base confusion — design + fix together with shared minus-strand fixtures.** | bam_processor.py, false_junction_filter.py | HIGH |
| **S2** | **NEW-075** integrate dT-primed cDNA poly-A walkback into `rectify correct` behind `--dT-primed-cDNA`; emit **gene/RNA strand** (not BAM strand) in TSV; port+harden `11_polya_walkback_recompute.py`; add regression fixture | correct_command.py; spec `ISSUE_walkback_integration_retrospective.md` | HIGH |
| **S3** | `merge_corrected_tsvs` tie-breaker: make `_n_agree` chrom-aware on `(chrom, corrected_3prime)`; multi-aligner position consensus must beat single-aligner outlier (mapq is NOT cross-aligner) | `consensus/corrected_consensus.py` | MED |
| **S4** | **Terminal Junction Peel (Module 2F)** = TODO "5' soft-clip rescue sequence-based matching". Opt-in flag; gated rollout per spec §Suggested Rollout (run 2H-bypass tests first) | `splice/splice_aware_5prime.py`, `bam_processor.py`; spec `terminal_junction_peel_2f_plan.md` | HIGH |
| **S5** | **C-II.2** dynamic shared-region-queue (Axis B work-stealing) + parallel prescans follow-up (verify pysam `fetch` releases GIL first). Deferred to "after Commit D" | `run/stages.py`, `bam/parallel.py`, poss. new `correct/region_dispatcher.py` | MED (perf) |
| **S6** | **Commit F** NETSEQ region-parallel — **GATED**: implement only if profile shows NETSEQ ≥10% wall (plan §F) | new `bam/netseq_bam_parallel.py`, `netseq_command.py` | LOW (perf, gated) |
| **S7** | **Commit D** analyze partial-streaming + `merge_corrected_tsvs` parallelism — **conditional** on Commit-Zero-B timing (if merge <10% wall, defer) | new `analyze/streaming.py`, analyze_command.py | MED (perf, gated) |
| **S8** | Bedgraph + genomic-distribution in manifest mode (`_run_analyze_manifest`) | analyze_command.py | MED (sequence w/ analyze work) |
| **S9** | Regenerate `rectify/data/validation/corrected_3ends.tsv` + review inverted indel-correction tests (`test_indel_correction.py`) — **after** S1/S2/S4 land (they change outputs) | validation set, tests | MED (last) |

**Out of scope for v1.0.0 (defer):** TODO "Evaluate newer aligners (Minisplice/GLASS/Winnowmap2)" — additive research, not a release blocker. TODO "deSALT on Sherlock conda install" is cluster-ops, not code (handle during cluster-sync step if deSALT needed in production panel).

---

## 4. Cluster validation (long-lead — Opus-orchestrated, poll-tight, START EARLY)

These run for hours and gate the v1.0.0 tag. **Kick off at session start** (after cluster sync) so wall-clock overlaps with §2/§3 code work. Poll per CLAUDE.md (5-min `ScheduleWakeup delaySeconds=270` until first heavy stage clears, then relax).

**"Both clusters" coverage:** V1+V2 exercise **H2** (heap-corruption + Axis-B acceptance) and V3 exercises **Sherlock** (lazy-consensus Lustre/NVMe staging) — different validation roles per cluster, satisfying Kevin's "production-scale on both Sherlock and H2" requirement.

| V | Validation | Setup | Gate it unblocks |
|---|---|---|---|
| **V1** | C-II.1 heap-corruption smoke — Han wt_R1 6.7M reads, H2 16-core (`--aligner-concurrency 4`) | BAM at H2 `.../han_2023_..._v0.9.0/results/wt_R1/wt_R1.rectified.bam`; confirm 6.7M-read DRS version | "Axis B safe at scale" — blocks C-II.2 (S5) |
| **V2** | C-II.1 acceptance gate — wall ≤ slowest_aligner_alone × 1.3 | same node as V1 | C-II.1 perf claim |
| **V3** | Lazy consensus Phase F — NVMe staging timing on Sherlock | pull `f7e6463`; reuse `wt_tfiiib_rep3/chunk_008`; expect ~83–100 s | confirms Lustre fix; production policy |

---

## 5. Sequencing & dependencies

```
SESSION OPEN
├─ GATE: confirm no concurrent sessions; get "OK to sync clusters"
├─ rsync M1(56bcee6) → H2 + Sherlock (.codex_backups first)        [Opus]
├─ START V1/V2/V3 cluster jobs ─────────────── poll in background  [Opus]
│
├─ DISPATCH Sonnet (edit-only, report diffs): P1,P2,P3,P4,P5,P6   [parallel]
│    └─ Opus stages+commits each atomically, in sequence
│
└─ Opus correctness work (sequential, while polling clusters):
     S1 (minus-strand cluster) → S3 (tie-breaker) → S2 (walkback) → S4 (2F peel)
        │
        ├─ when V1 clears: S5 (C-II.2)        [perf, gated on Axis-B-safe]
        ├─ profile decision: S7 (D streaming), S6 (F NETSEQ)  [perf, gated]
        ├─ S8 (manifest bedgraph)  S9 (regen validation + test review)  [after S1/S2/S4]
        └─ P7 doc consolidation (git mv) + Bucket-4 moves     [late]

RELEASE GATE → §6
```

**Hard orderings:** S5 after V1 passes. S9 after S1/S2/S4 (output-changing). S6/S7 gated on profile evidence (don't build speculatively). P7 after P1 (doc-path thrash). Bucket-4 doc moves after AGENT_FIXES/HANDOFF WIP committed.

---

## 6. Blocking gates (require explicit Kevin approval)

1. **Push** — only `56bcee6` (E.3) is unpushed. Need explicit "OK to push" + confirm no concurrent sessions. (Main `HANDOFF.md` claim "C-II.1 not pushed" is stale — C-II.1 is already on origin.)
2. **Cluster sync** — rsync M1→H2/Sherlock before any production test (clusters behind M1; M1 authoritative).
3. **v1.0.0 release** — only after V1+V2+V3 pass AND S1–S4 (correctness) land. Then: bump version, finalize CHANGELOG/Commit G docs, push, tag `v1.0.0`.
4. **Surgical staging always** — explicit paths; never `git add -A`/`.`; atomic stage+commit per concurrent-session rule.
5. **Per-commit test convention** — every commit touching `rectify/core/bam/`, `consensus/`, `commands/correct*`, or `commands/run/*` runs the ~148-test blast-radius slice (`pytest -m "not slow"` on the affected test files); full `pytest -m "not slow"` (~1 min) before the push in gate 1.

---

## 7. Open verification carried forward
- **NEW-072** status not fully resolved — P4 must READ multi_aligner.py run_ultra/run_desalt Popen stderr handling before deciding fix-vs-noop.
- **Commit-Zero-B full-sample timing** never recorded — needed to scope S6 (F) and S7 (D). Run a profiling pass before committing to either perf item.
