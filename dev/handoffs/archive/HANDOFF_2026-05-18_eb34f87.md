# Session Handoff — UMI-bin stratified calibration Phase D complete; Phase C next

**Date:** 2026-05-18 (afternoon/evening)
**Branch:** working tree (nothing committed this session — all changes are `[uncommitted]`)
**Top of branch:** `accc7df` (docs: cat3 cleanup + queue ambiguity-window design note)
**Prior handoff:** `handoffs/HANDOFF_2026-05-18_accc7df.md` (validation suite 40→0)

---

## 1. What was done

- **Phase D Option 1 — sidecar BAM lookup** added to
  `scripts/calibration/empirical_cigar_error_profiler.py`:
  - New `--umi-source-bam PATH` CLI flag. At startup, builds
    `umi_lookup: dict[str, int]` from the sidecar BAM (applying
    `_normalise_read_name` to keys — critical for matching load_reads
    normalisation). Passes through `_worker` and `profile_chunk` via
    new `umi_lookup=` kwarg.
  - In `profile_chunk`: sidecar dict consulted first; falls back to
    inline `read.get_tag(umi_tag)` when sidecar is absent (QSrev path
    unchanged). Per-chunk hit-rate logged: `Sidecar UMI lookup: N hits /
    M total (P% hit rate)`.
  - Removed `include_groups=False` from `derive_penalties` groupby
    (pandas 1.5.1 compat fix — that kwarg requires ≥2.2).

- **Sherlock job 25334819** — COMPLETED 0:0, 1h 14m 53s, sh03-07n10.
  Sidecar lookup built 999,852 entries. Per-bin event counts:
  umi1 = 399,644 reads / 368M events, umi2 = 58,166 / 74M,
  umi3+ = 25,605 / 35M. All 8 chunk alignments used skip-if-exists.

- **Four penalty tables bundled** into
  `rectify/data/genomes/saccharomyces_cerevisiae/penalty_tables/`:
  - `penalty_scores_cdna.tsv` — 125 rows (pooled, 477M events, 8 chunks)
  - `penalty_scores_cdna_umi1.tsv` — 125 rows (singleton reads)
  - `penalty_scores_cdna_umi2.tsv` — 117 rows (doubleton consensus)
  - `penalty_scores_cdna_umi3plus.tsv` — 106 rows (≥3-read consensus)

- **New test** `test_sidecar_bam_lookup_routes_per_bin` in
  `tests/test_profiler_umi_binning.py`: synthetic sidecar + untagged
  per-aligner BAMs; asserts correct bin routing via `umi_lookup`.

- **Fallback test flipped** in `tests/test_data_bundling.py`:
  `test_cdna_per_bin_falls_back_to_pooled_when_file_absent` →
  `test_cdna_per_bin_resolves_to_specific_file` (now asserts the
  per-bin file exists and is returned directly).

- **Convergence check** run and saved to
  `scripts/calibration/runs/error_profile_cdna_20260518/CONVERGENCE.md`:
  median |Δ%| = umi1 3.12%, umi2 3.03%, umi3+ 1.98% across 8 chunks.
  All three bins: 8 chunks sufficient.

---

## 2. What's verified

- `pytest tests/test_data_bundling.py tests/test_profiler_umi_binning.py -v`
  → **30 passed, 0 failed** (M1, post-bundling, post-test flip).
- Four penalty TSVs exist on disk and pass the resolver test.
- `python scripts/calibration/bundle_protocol_tables.py --help` shows
  `--umi-bin` flag.
- Resolver verified: `bin='umi1'` → `penalty_scores_cdna_umi1.tsv`
  (direct hit, no longer falls back to pooled).
- Sidecar hit-rate 999,852/999,852 in Sherlock log (all reads in
  `stage1_consensus.bam` had XC tags).

NOT VERIFIED:
- The Phase C integration (`junction_refiner.py`) is entirely unwritten.
  The per-bin tables exist on disk; nothing yet tells `rectify correct`
  to pick among them.
- Full test suite (`pytest -m "not slow"`) not re-run this session —
  no production code was modified, so no regression risk, but worth
  one clean run before Phase C lands.

---

## 3. Open items

- **Phase C — `rectify correct` integration** (deferred from prior
  sessions): load all bin-specific penalty tables at startup in
  `junction_refiner.py`; per-read UMI tag lookup; route to the right
  table via a `PenaltyTableSet` class. Spec in
  `docs/handoffs/cdna_umi_stratified_calibration.md` §3.3. Estimated
  4–6 h; deserves its own session with fresh context on
  `junction_refiner.py`. Deferred because it's surgical on load-bearing
  Module 2H — not safe to start at the tail end of a long calibration
  session.

- **Phase E — docs + integration smoke test**: `penalty_tables/README.md`
  update; `docs/CLAUDE.md` "Empirical Penalty Tables" section; smoke test
  that `rectify correct --dT-primed-cDNA` on a synthetic XC-tagged BAM
  picks different tables per read. Estimate 1 h. Deferred until Phase C
  lands.

- **Unrelated open work** from prior HANDOFF (validation suite):
  cat3_plus_2 + strand mirror; cat2_minus_2 geometry decision; Cat9
  Module 2H scope. All documented in `docs/handoffs/debugger_queue.md`;
  no regression — still 0 failures in the validation suite.

- **`include_groups` on Sherlock**: the profiler fix removed the kwarg
  (pandas 1.5.1 compat). Sherlock's rectify env has a newer pandas and
  was unaffected by this bug. Consistent now — no action needed.

---

## 4. Resume

**Resume:** start Phase C. Before writing any code:

1. Read `docs/handoffs/cdna_umi_stratified_calibration.md` §3.3 "Phase C"
   (lines ~118–131) for the full spec: `PenaltyTableSet` class, per-read
   tag lookup, `_score_junction()` call site, smoke test.

2. Find the current `junction_refiner.py` entry point — run:
   ```bash
   grep -n "penalty_table\|junction_penalty\|load.*table\|PenaltyTable" \
       rectify/core/correct/junction_refiner.py | head -30
   ```
   Locate how the table is currently loaded at startup and passed into
   `_score_junction()`. That determines the refactor surface area.

3. Check whether `XC` (the UMI count tag) is visible at the point
   `junction_refiner.py` scores junctions — it lives on the
   `stage1_consensus.bam` read, and `rectify correct` accepts the
   consensus BAM. Confirm the tag propagates through to
   `junction_refiner.py`'s per-read context; if not, the sidecar-lookup
   pattern from the profiler is the fallback (same `umi_lookup` dict
   built at startup from the input BAM).

4. Implement `PenaltyTableSet` and wire it in. Keep the existing
   single-table API intact for back-compat (DRS and QSrev callers).

5. Run `pytest tests/test_junction_refiner.py tests/test_data_bundling.py
   tests/test_profiler_umi_binning.py -v` — expect all green.

If XC is NOT visible in `junction_refiner.py`'s per-read loop, surface
that finding before implementing — it changes the architecture (the
sidecar pattern adds an O(N) BAM scan at startup vs the ideal inline
tag read).

---

## 5. Files touched

**`[uncommitted]` — working tree, this session:**

- `scripts/calibration/empirical_cigar_error_profiler.py` — sidecar
  lookup (`--umi-source-bam`), `umi_lookup` threading through `_worker`
  + `profile_chunk`, hit-rate log, pandas 1.5.1 compat fix
- `scripts/calibration/run_profiler_cdna_sherlock.sh` — added
  `--umi-source-bam "$CONS_BAM"` to per-chunk loop + pooled invocation
- `tests/test_profiler_umi_binning.py` — new file (11 tests; was new
  from Phase A, extended with sidecar test this session)
- `tests/test_data_bundling.py` — fallback test flipped to positive
  assertion (`test_cdna_per_bin_resolves_to_specific_file`)
- `rectify/data/genomes/saccharomyces_cerevisiae/penalty_tables/penalty_scores_cdna.tsv`
  — 8-chunk pooled (125 rows; was 2-chunk at prior session end)
- `rectify/data/genomes/saccharomyces_cerevisiae/penalty_tables/penalty_scores_cdna_umi1.tsv`
  — new, 125 rows
- `rectify/data/genomes/saccharomyces_cerevisiae/penalty_tables/penalty_scores_cdna_umi2.tsv`
  — new, 117 rows
- `rectify/data/genomes/saccharomyces_cerevisiae/penalty_tables/penalty_scores_cdna_umi3plus.tsv`
  — new, 106 rows
- `rectify/data/__init__.py` — nested cdna bin resolver (Phase B,
  carried from prior session)
- `scripts/calibration/bundle_protocol_tables.py` — `--umi-bin` flag
  (Phase B, carried from prior session)
- `scripts/calibration/runs/error_profile_cdna_20260518/` — Sherlock
  results rsynced locally (gitignored run directory)
- `docs/handoffs/cdna_umi_phase_d_resume.md` — consumed; can be
  deleted or archived once Phase C lands

**Prior-session `[uncommitted]` still in tree (not touched this session):**
- `scripts/calibration/run_profiler_qsrev_h2.sh` — `module load bwa` fix
- `scripts/calibration/run_profiler_qsrev_sherlock.sh` — Sherlock wrapper
- `rectify/data/genomes/saccharomyces_cerevisiae/penalty_tables/penalty_scores_qsrev.tsv`
- `rectify/data/genomes/saccharomyces_cerevisiae/penalty_tables/README.md`
- `tests/test_profiler_umi_binning.py` — was new from Phase A
- The large `??` untracked items in `git status` (figures, docs, rebuild
  scripts) are pre-existing WIP — do not commit without explicit
  instruction.
