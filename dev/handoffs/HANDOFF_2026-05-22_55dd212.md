# Handoff — WIP Resolver Brief

**Date:** 2026-05-22  
**Branch:** `drs-validation-rebuild`  
**HEAD:** `9f998b4` — `fix(analyze): add missing cluster_gene_attribution module`  
**Scope:** Commit C full concurrency queue (Opus-owned) + Commits D–G chain.

---

## 1. What was done (this session — 2026-05-22, continuation)

Four additional commits landed on top of `1ab71f0` and pushed to
`origin drs-validation-rebuild`:

- **`3cff921`** — `chore(docs): reorganize dev/ directory structure`  
  Moved `handoffs/`, `docs/handoffs/` → `dev/handoffs/`; `CODEX_AUDIT.md` →
  `dev/audits/`; `PLOT_SKILLS.md`, `TODO.md` → `dev/`; `generate_*.py` →
  `scripts/figures/`. Deleted `CODEX.md`, `README_KR_edits.md` stubs.

- **`d3ed8b5`** — `docs: merge README_KR_edits, update CLAUDE.md paths, add consolidation proposal`  
  Also contains **Commit C preparatory code** (mislabeled in subject line):
  - `_run_correction` now accepts either `corrected_reads.manifest.tsv` (Commit B
    default) or `corrected_reads.tsv` (legacy `--emit-merged-tsv`). Returns the
    path of whichever artifact exists.
  - `_per_aligner_canonical_output(aligner_output_dir)` helper centralizes that
    check for both resume skip-check and post-call success validation.
  - `_run_correction_per_aligner`: plumbs `resolve_aligner_concurrency` from
    `rectify.core.utils.resources`; logs resolved budget. **Loop stays
    sequential** — actual queue pending.
  - New tests: `tests/test_run_correction_per_aligner.py` (8 tests, all passing).

- **`9f998b4`** — `fix(analyze): add missing cluster_gene_attribution module`  
  Committed `cluster_gene_attribution.py` (474 lines) and the matching
  `__init__.py` exports. Fixes the broken import in `manifest.py` (which had
  been committed at `1ab71f0` importing a file that wasn't included). Fresh
  `git clone` now succeeds.

---

## 2. What's verified

- New tests `tests/test_run_correction_per_aligner.py`: **8 passed** (isolated run).
- `git ls-remote origin drs-validation-rebuild | awk '{print substr($1,1,7)}'` → `9f998b4` ✓
- Full fast suite: last clean run on `1ab71f0` was **1219 passed, 35 skipped, 4 deselected**.
  Three flaky failures in `test_run_command_wiring.py` appeared in one run due to test-order
  sensitivity; they pass in isolation and are pre-existing.
- H2 and Sherlock are rsync copies (no `.git`) — **not yet synced past `1ab71f0`**. Pull before
  cluster work.

---

## 3. Open items

- **Commit C: aligner-queue concurrency refactor** — The preparatory plumbing is
  committed (`d3ed8b5`). What remains: replace the sequential `for aligner_name in ...` loop
  in `rectify/core/commands/run/stages.py` (now ~360–430) with a shared
  `multiprocessing.Queue` of `(aligner_name, region)` tasks that consumes the
  already-resolved `_resolved_ac` budget. Spec: `dev/specs/briefings/commit_c_briefing.md`
  (untracked, readable in working tree).  
  **Design note:** HP scoring dominates wall time (64%); mapPacBio is 2.6× slower than
  minimap2 per read — queue must tolerate heterogeneous aligner cost.  
  **Requires Opus review + cluster validation** (Han wt_R1, 16-core H2 node,
  target: ≤ slowest_aligner_alone × 1.3). Do NOT implement on Sonnet without Opus sign-off.

- **Non-chunked `run` path never calls `write_corrected_consensus_bam`** — `run/single_sample.py`
  bypasses the lazy path. Deferred: needs explicit design decision before touching
  `single_sample.py`. See `dev/specs/lazy_corrected_consensus_audit_20260521.md` §"Open items".

- **Commits D–G not started** — analyze sidecar wiring (D), DRS trim+align+cDNA stage-1 (E),
  NETSEQ (F), resume integration tests (F.5), docs+CHANGELOG (G).  
  Full chain: `dev/specs/briefings/commit_cdefg_briefing.md` (untracked, do NOT commit).

---

## 4. Resume command

**Sync clusters first:**
```bash
# H2 and Sherlock are rsync copies — update both before any cluster work
ssh hoffman2 'bash -lc "cd /u/home/k/kevinroy/software/rectify && git pull origin drs-validation-rebuild"'
ssh sherlock 'bash --norc --noprofile -c "cd /oak/stanford/groups/larsms/Users/kevinroy/software/rectify && git pull origin drs-validation-rebuild"'
```

**Commit D (Sonnet-owned — immediate next task):**
1. Read `dev/specs/briefings/commit_cdefg_briefing.md` §"Commit D" for scope.
2. Wire `can_skip_stage` at entry + `write_stage_sidecar` at success exit in
   `_run_analyze_manifest` (`rectify/core/analyze/manifest.py:130`) and `run_analyze`
   (`rectify/core/commands/analyze_command.py:82`).
3. Write `tests/test_analyze_sidecar.py`: write-on-success, skip-on-re-entry with
   matching inputs, recompute when inputs change.
4. Run `pytest -m "not slow"` on M1 before staging. Stage explicit paths only, then commit.
5. Defer: `streaming.py` `ChromShard` helpers (needs cluster validation) and two-pass
   cluster-calling refactor (Opus-owned, correctness-critical).

**Commit C (Opus-owned — do not implement on Sonnet alone):**
1. Read `dev/specs/briefings/commit_c_briefing.md` (untracked, readable) for full design.
2. Read `rectify/core/commands/run/stages.py` near `_run_correction_per_aligner` —
   `_resolved_ac` is already computed; implement the Queue consumer loop.
3. Validate on H2: Han wt_R1, 5 aligners, 16-core node. Wall target ≤ slowest_aligner_alone × 1.3.
4. Run `pytest -m "not slow"` on M1 before staging. Then stage + commit + push.

**Hard rules for the resolver:**
- NEVER `git add -A` or `git add .` — working tree has WIP for multiple upcoming commits.
- ALWAYS run `git diff --cached --stat` before `git commit` to verify only intended files are staged.
- NEVER commit on a cluster — M1 is the git source of truth.
- Run `pytest -m "not slow"` before each commit touching Python source.
- Branch is `drs-validation-rebuild`. `master` is frozen at 0.9.0 public release.

---

## 5. Remaining uncommitted files

### Deferred to Commit D

(no Python WIP files pending for analyze — hotfix committed in `9f998b4`)

### Deferred to Commit C or later

- `rectify/core/bam/bam_writer.py` — WIP modifications (review scope before staging)
- `rectify/core/bam/read_edits.py` — WIP modifications (review scope before staging)
- `tests/test_read_edits_reanchor.py` [untracked] — WIP test for read_edits; 2 failures
  noted in `d3ed8b5` commit message as pre-existing; review before staging

### Validation data (stage with source commits)

- `rectify/data/validation/PROVENANCE.json`, `README.md`, `VALIDATION_READS.md`
- `rectify/data/validation/aligners/*.bam`, `*.bam.bai` (all 5 aligners, rebuilt)
- `rectify/data/validation/rectified/per_aligner/*.pA_rest.bam` (+ .bai) [untracked]
- `rectify/data/validation/rectified/per_aligner/minimap2_unrectified.bam` [untracked]
- `rectify/data/validation/corrected_reads_index.bed.gz` [untracked]
- `rectify/data/validation/corrected_reads_stats.tsv` [untracked]

### Spec / briefing files (untracked — do NOT commit to main branch)

- `dev/specs/briefings/commit_cdefg_briefing.md` — Commit C–G roadmap
- `dev/specs/lazy_corrected_consensus_audit_20260521.md` — P0/P1/P2/P3 bug audit
- `dev/specs/lazy_corrected_consensus_plan.md` — lazy consensus design
- `dev/specs/lazy_consensus_timing_20260521.md` — Sherlock timing results
- `dev/specs/terminal_junction_peel_2f_plan.md` — junction peel design

### Other modified tracked files (review before staging)

- `AGENT_FIXES.md` — has uncommitted P0–P3 entries; stage with source commits
- `scripts/timing/` [untracked] — timing benchmark scripts; review before staging
