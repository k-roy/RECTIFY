================ C6 VARIANT-AWARE-JUNCTION HEADROOM (decoder-free) ================
reps=60  seed=7  aligner=minimap2 -ax splice -uf --eqx

(a) BELOW-CEILING — SPLICE_MIMIC_DEL drivers (truth = DELETION, no intron)
    drivers n=180  placed=180
    ceiling (D placeable, read lacks block by construction)   = 1.0000
    incumbent (mm2 emits the truth D, NO intron)              = 0.0000 (0/180)
    HEADROOM (mm2 fabricates a variant-adjacent FP INTRON)    = 1.0000 (180/180)
    scorer fp_variant_adjacent (shipped metric)               = 180
    scorer indel position-exact concordance on the deletion   = 0.0000

(b) ZERO-EVIDENCE NM test — is mm2's FP intron a 0-mismatch alignment?
    driver FP introns with NM==0 (== the truth D is also 0-mm) = 180/180
    => the intron and the deletion are the SAME query at 0 mismatches; no read-
       level evidence separates them (construction-confirmed, not load-bearing).

(c) THE TENSION (load-bearing) — length-matched REAL introns (truth = intron, N)
      len  drivers  mm2_FP_N  realIntr  mm2_TP_N
       40       60        60        60        60
       60       60        60        60        60
      100       60        60        60        60
    => within EACH length, mm2 emits the IDENTICAL N-op for the driver (FP) and
       the real intron (TP). A 'long GT..AG gap -> D' rule recovers the 180
       driver FPs but converts the 180 matched real introns to FN, ~1:1.
       Drivers and real introns share one read-feature cell (length, GT..AG,
       random interior) -> NO function of the read separates them (zero-evidence).

(d) SPECIFICITY (positive) — mm2 is NOT blunt; it retains real junctions
    SNP_NEAR_JUNC real-junction recall  = 0.9833 (fp_variant_adjacent=1)
    matched real-intron recall          = 0.9889  (mm2 emits N on 180/180 placed; NM==0 on 178)
    short-deletion (<40bp) control: stays D (smoke (E) verified) n=60

---- VERDICT (pre-committed) ----
  HEADROOM=1.0000 > 0 (mm2 IS below ceiling, fabricates 180 variant-
  adjacent FP introns) BUT every driver FP intron is NM==0 == the truth D (0-mismatch),
  AND the ONLY read-level recovery rule (long GT..AG gap -> D) FNs the length-matched
  real introns ~1:1 (180 real introns mm2 correctly calls N). The drivers are
  the ZERO-EVIDENCE trap: indistinguishable from real cryptic introns at the read level.
  The only deciding input is an EXTERNAL variant catalog (matched VCF) — which on THIS
  corpus == the truth label (circular).
  => C6-as-read-evidence REFUTED on this corpus. DEFER a VCF-integration residue to a
     MEASURED real-data trigger (matched germline VCF as INDEPENDENT evidence). Do NOT
     build a variant-aware emission against this corpus; do NOT ship a blunt
     'relabel/abstain near any variant' rule (specificity (d) shows it suppresses real
     junctions). C6 joins the gate-refuted facets.
